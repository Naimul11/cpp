#include <bits/stdc++.h>
using namespace std;
#define ll long long
#define fl(n) for (int i = 0; i < n; i++)
#define flj(n) for (int j = 0; j < n; j++)
#define ap(vec) for_each(vec.begin(), vec.end(), print)
void print(int i) cout <<' ' << i;
int main()
{

ll x,y,z,a,b,c,ans,count=0;
 

 return 0;
}
